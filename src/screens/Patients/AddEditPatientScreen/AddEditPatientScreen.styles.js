import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    textAlign: "center",
    fontSize: 32,
    fontWeight: "bold",
  },
  addButton: {
    backgroundColor: "#001E4C",
    margin: 20,
  },
});
